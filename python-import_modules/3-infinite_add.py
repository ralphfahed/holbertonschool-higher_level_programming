#!/usr/bin/python3
import sys
#result of addition of all argument folowed by a new line , we can use int ta 7ael la integers
def sum_all(*args):
    sum = 0
    for i in args:
        if not str(num).isdigit():
            return False
        else:
            sum += int(num)
